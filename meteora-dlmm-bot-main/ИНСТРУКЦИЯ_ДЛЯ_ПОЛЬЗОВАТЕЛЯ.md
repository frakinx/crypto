# 📖 Инструкция по установке и запуску Meteora DLMM Bot

Эта инструкция поможет вам установить и запустить бота, даже если вы никогда не программировали.

---

## 📋 Что нужно установить перед началом

### Шаг 1: Установка Node.js

**Node.js** — это программа, которая позволяет запускать бота на вашем компьютере.

1. Откройте браузер и перейдите на сайт: **https://nodejs.org/**
2. На главной странице вы увидите две большие зеленые кнопки:
   - **LTS** (рекомендуется) — стабильная версия
   - **Current** — последняя версия
3. Нажмите на кнопку **LTS** — она автоматически начнет скачивание
4. После скачивания запустите установщик (файл будет называться примерно `node-v20.x.x-x64.msi`)
5. В установщике просто нажимайте **"Next"** (Далее) на всех шагах — все настройки по умолчанию подходят
6. После установки нажмите **"Finish"** (Готово)

**Проверка установки:**
- Нажмите клавиши `Win + R` (Windows) или откройте программу "Терминал" (Mac/Linux)
- Введите команду: `node --version`
- Нажмите Enter
- Если вы увидите что-то вроде `v20.x.x` — значит все установлено правильно ✅

---

## 📦 Установка бота

### Шаг 2: Открытие папки с ботом

1. Найдите папку с ботом (она называется `meteora-dlmm-bot-main`)
2. Откройте эту папку
3. В адресной строке папки (вверху) нажмите один раз, чтобы выделить путь
4. Введите `cmd` и нажмите Enter — откроется черное окно (командная строка)

**Альтернативный способ:**
- Нажмите правой кнопкой мыши на папку с ботом
- Выберите "Открыть в терминале" или "Open in Terminal"

### Шаг 3: Установка зависимостей

В открывшемся черном окне (командной строке) введите следующую команду и нажмите Enter:

```
npm install
```

**Что происходит:** Бот скачивает все необходимые программы и библиотеки из интернета. Это может занять 2-5 минут.

**Вы увидите:**
- Много текста прокручивается в окне
- В конце должно появиться что-то вроде: `added 234 packages` или `up to date`
- Если появилась ошибка — см. раздел "Решение проблем" ниже

---

## ⚙️ Настройка бота

### Шаг 4: Создание файла настроек

1. В папке с ботом создайте новый файл с именем `.env` (обратите внимание на точку в начале!)
   - Если Windows не позволяет создать файл с точкой в начале, создайте файл `env.txt`, затем переименуйте его в `.env`
   - Или используйте командную строку: введите `copy nul .env` и нажмите Enter

2. Откройте файл `.env` в блокноте (Notepad) или любом текстовом редакторе

3. Скопируйте и вставьте следующий текст:

```

RPC_URL=https://mainnet.helius-rpc.com/?api-key=f1ebee20-d1aa-46f8-b5b9-64f3ab23db3f


# Приватный ключ кошелька (формат base58 или JSON-массив)
# ⚠️ ВАЖНО: Никогда не делитесь этим ключом с кем-либо!
# Формат base58: длинная строка из букв и цифр
# Формат JSON: [1,2,3,4,5,...] (массив из 64 чисел)
WALLET_SECRET_KEY=your_wallet_secret_key_here

## Jupiter API
QUOTE_API_BASE=https://quote-api.jup.ag
JUP_API_KEY=          # можно оставить пустым, если публичный доступ

# Meteora DLMM
DLMM_API_BASE=https://dlmm-api.meteora.ag


# Лимиты и настройки сделок
MAX_TRADE_QUOTE=0.5         # в SOL (пока)
SLIPPAGE_BPS=100            # 1%
DEX_WHITELIST=Orca,Raydium  # опционально
```

4. **ВАЖНО:** Замените `ваш_секретный_ключ_кошелька_здесь` на ваш реальный секретный ключ кошелька
   - **НИКОГДА НЕ ДЕЛИТЕСЬ ЭТИМ КЛЮЧОМ С НИКЕМ!** Это ваш пароль от кошелька

5. Сохраните файл (Ctrl+S) и закройте его

**Где взять секретный ключ (два варианта):**

**Вариант 1: Если у вас есть файл кошелька Solana (JSON файл)**
- Откройте файл кошелька в блокноте
- Найдите массив чисел в квадратных скобках, например: `[1,2,3,4,5,...]` (64 числа)
- Скопируйте весь массив вместе с квадратными скобками
- Вставьте в файл `.env` вместо `ваш_секретный_ключ_кошелька_здесь`
- Пример: `WALLET_SECRET_KEY=[1,2,3,4,5,...]`

**Вариант 2: Если у вас есть строка ключа (Base58)**
- Это длинная строка из букв и цифр, например: `5KJvsngHeMoi...`
- Скопируйте всю строку целиком
- Вставьте в файл `.env` вместо `ваш_секретный_ключ_кошелька_здесь`
- Пример: `WALLET_SECRET_KEY=5KJvsngHeMoi...`

**Как создать новый тестовый кошелек (если у вас нет):**
- Используйте Phantom или другой кошелек Solana
- Экспортируйте приватный ключ (будьте осторожны!)
- Или используйте онлайн генератор (только для тестовых кошельков!)

---

## 🚀 Запуск бота

### Шаг 5: Запуск

В том же черном окне (командной строке), где вы запускали `npm install`, введите:

```
npm run server
```

Нажмите Enter.

**Что должно произойти:**
- Появится много текста в окне
- Вы увидите строки вроде:
  ```
  [WEB] 🚀 Сервер запущен на http://localhost:3001
  [BOT] Starting Meteora Position Monitor Bot...
  [BOT] Bot pubkey: E3KJC2D4QJvG2RFeLi6qABDA2y4nUpP8fSnRxqwe7sC7
  [BOT] Starting position monitoring...
  ```
- Бот начнет работать! ✅

### Шаг 6: Открытие веб-интерфейса

1. Откройте браузер (Chrome, Firefox, Edge — любой)
2. В адресной строке браузера введите: `http://localhost:3001`
3. Нажмите Enter
4. Должен открыться веб-интерфейс бота

**В веб-интерфейсе вы можете:**
- Просматривать все доступные пулы Meteora
- Открывать новые позиции
- Управлять существующими позициями
- Настраивать параметры бота

---

## 🛑 Остановка бота

Чтобы остановить бота:
1. Вернитесь в черное окно (командную строку), где запущен бот
2. Нажмите клавиши `Ctrl + C`
3. Подтвердите остановку, если спросит (нажмите `Y` и Enter)

---

## ❓ Решение проблем

### Проблема: "npm не является внутренней или внешней командой"

**Решение:** Node.js не установлен или не добавлен в PATH.
1. Переустановите Node.js (см. Шаг 1)
2. После установки **перезагрузите компьютер**
3. Попробуйте снова

### Проблема: "Cannot find module" или "Error: Cannot find package"

**Решение:** Зависимости не установлены правильно.
1. В командной строке введите: `npm install`
2. Подождите завершения установки
3. Попробуйте запустить бота снова

### Проблема: "Port 3001 is already in use"

**Решение:** Порт уже занят другим приложением.
1. Остановите бота (Ctrl+C)
2. Подождите 10 секунд
3. Запустите снова: `npm run server`

### Проблема: "Invalid secret key" или ошибки с кошельком

**Решение:** Неправильный формат секретного ключа.
1. Проверьте файл `.env`
2. Убедитесь, что ключ скопирован полностью (без пробелов в начале/конце)
3. Если ключ в формате массива `[1,2,3,...]`, оставьте его как есть
4. Если ключ в формате строки, убедитесь что он в кавычках: `WALLET_SECRET_KEY="ваш_ключ"`

### Проблема: Бот не видит позиции или не работает

**Решение:** Проверьте логи в командной строке.
- Красные строки с "Error" — это ошибки
- Прочитайте текст ошибки — там обычно написано что не так
- Убедитесь, что:
  - Интернет подключен
  - Кошелек имеет средства (SOL для комиссий)
  - RPC_URL правильный (если используете свой RPC)

---

## 📞 Дополнительная помощь

Если что-то не работает:
1. Скопируйте текст ошибки из командной строки
2. Сделайте скриншот экрана
3. Отправьте тому, кто дал вам бота

---

## 🔐 Безопасность

⚠️ **ВАЖНО:**
- **НИКОГДА** не делитесь файлом `.env` с другими людьми
- **НИКОГДА** не публикуйте свой секретный ключ в интернете
- Используйте только **тестовый кошелек** для первых запусков
- Храните резервную копию секретного ключа в безопасном месте

---

## 📝 Краткая памятка

1. Установить Node.js → https://nodejs.org/
2. Открыть папку с ботом в командной строке
3. Выполнить: `npm install`
4. Создать файл `.env` с настройками
5. Запустить: `npm run server`
6. Открыть в браузере: `http://localhost:3001`

**Готово! Бот работает! 🎉**

