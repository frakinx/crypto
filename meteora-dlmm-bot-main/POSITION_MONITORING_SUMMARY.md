# Резюме реализации модуля мониторинга позиций

## ✅ Что было реализовано

### 1. Модульная архитектура (`src/position-monitoring/`)

Создана полная система мониторинга и управления позициями в пулах Meteora DLMM:

#### **config.ts** - Конфигурация админа
- ✅ Параметры коридора (верхний/нижний процент от текущей цены)
- ✅ Параметры stop loss (-2% от нижней границы)
- ✅ Параметры проверки fee (процент от нижней границы)
- ✅ Параметры take profit (±2%)
- ✅ Параметры автоподбора пула (ликвидность, объем, bin step)
- ✅ Параметры мониторинга (интервалы проверки)
- ✅ Параметры Mirror Swapping стратегии

#### **types.ts** - Типы данных
- ✅ `PositionInfo` - полная информация о позиции
- ✅ `PositionDecision` - решение по позиции (close/keep/open_new/hedge/none)
- ✅ `PriceUpdate` - обновление цены
- ✅ `FeeVsLossCalculation` - расчет fee vs losses

#### **priceMonitor.ts** - Мониторинг цены
- ✅ Получение текущей цены пула через SDK
- ✅ Кэширование цен (TTL 10 секунд)
- ✅ Альтернативный метод через Jupiter Price API
- ✅ Проверка границ позиции (верхний/нижний потолок)
- ✅ Расчет позиции цены относительно границ
- ✅ Проверка уровня для проверки fee

#### **strategyCalculator.ts** - Расчет стратегии
- ✅ Расчет накопленных комиссий
- ✅ Сравнение fee с потерями от stop loss
- ✅ Расчет цены безубыточности
- ✅ Расчет hedge amount для Mirror Swapping
- ✅ Оценка стоимости позиции

#### **positionManager.ts** - Управление позициями
- ✅ Открытие новых позиций с автоматическим расчетом границ
- ✅ Закрытие позиций
- ✅ Принятие решений по позициям на основе стратегии:
  - Проверка верхнего потолка → закрытие
  - Проверка при падении цены → расчет fee vs losses
  - Автозакрытие по SL если fee перекрывают потери
  - Открытие новой позиции ниже если fee не перекрывают
- ✅ Сохранение/загрузка позиций из хранилища

#### **poolSelector.ts** - Автоподбор пула
- ✅ Поиск пулов по токенам
- ✅ Оценка пулов по критериям:
  - Ликвидность
  - Объем за 24ч
  - Bin step
  - Комиссии
- ✅ Подбор пула для новой позиции

#### **monitor.ts** - Главный модуль мониторинга
- ✅ Объединение всех компонентов
- ✅ Цикл мониторинга позиций (настраиваемый интервал)
- ✅ Выполнение решений по позициям
- ✅ Управление жизненным циклом (start/stop)

#### **storage.ts** - Хранилище позиций
- ✅ Сохранение позиций в JSON файл (`data/positions.json`)
- ✅ Загрузка позиций при старте
- ✅ Обновление статусов позиций

### 2. Расширение `src/dex/meteora.ts`

Добавлены функции для работы с позициями:
- ✅ `createClosePositionTransaction` - создание транзакции закрытия позиции
- ✅ `getPositionInfo` - получение информации о позиции
- ✅ `getClaimableSwapFees` - получение накопленных комиссий

### 3. API Endpoints в `src/server.ts`

Добавлены endpoints для управления:
- ✅ `GET /api/admin/config` - получить конфигурацию админа
- ✅ `POST /api/admin/config` - сохранить конфигурацию админа
- ✅ `POST /api/meteora/close-position` - закрыть позицию

## 📋 Логика работы (как требовалось)

### 1. Мониторинг после открытия позиции

После открытия позиции запускается автоматический мониторинг:

1. **Проверка верхнего потолка (Take Profit)**
   - Если цена >= верхней границы → **закрываем позицию**

2. **Проверка при падении цены**
   - Если цена падает и достигает уровня проверки fee (например, 50% от нижней границы):
     - Рассчитываем накопленные комиссии
     - Рассчитываем потери при закрытии на stop loss (-2% от нижней границы)
     - **Если fee >= потери** → закрываем позицию по SL
     - **Если fee < потери** → оставляем позицию и открываем новую ниже

3. **Проверка нижней границы (Stop Loss)**
   - Если цена <= нижней границы → **закрываем позицию**

### 2. Автоподбор пула

На основе параметров админа:
- Коридор в процентах (верхний и нижний)
- Минимальная ликвидность
- Минимальный объем за 24ч
- Предпочтительный bin step

Система автоматически подбирает подходящий пул.

### 3. Mirror Swapping стратегия

Реализована основа для стратегии из презентации:
- Расчет hedge amount по формуле: `h = 0.5 · (P₀ − P)/P₀`
- Готовность к выполнению зеркальных свопов через Jupiter

## ⚠️ Что нужно доработать (требуется информация от вас)

### 1. **Получение реальных данных о комиссиях**

**Проблема:** Сейчас используется упрощенная модель расчета комиссий.

**Что нужно:**
- Как получать реальный объем торговли за 24ч для пула? (API endpoint?)
- Как получать реальную комиссию пула в bps?
- Как рассчитать процент ликвидности позиции от общей ликвидности пула?
- Нужно ли использовать `getClaimableSwapFees` из SDK для получения реальных комиссий?

**Где в коде:** `src/position-monitoring/positionManager.ts:185-200`

### 2. **Получение реальных данных о пуле**

**Проблема:** В `poolSelector.ts` используются данные из API, но нужно убедиться в правильности полей.

**Что нужно:**
- Правильные поля для ликвидности в API Meteora? (`liquidity`, `total_liquidity`, `tvl`?)
- Правильные поля для объема? (`trade_volume_24h`, `volume_24h`?)
- Нужны ли дополнительные источники данных (Birdeye, Jupiter)?

**Где в коде:** `src/position-monitoring/poolSelector.ts:70-90`

### 3. **Реализация Mirror Swapping**

**Проблема:** Расчет hedge amount есть, но выполнение swap не реализовано.

**Что нужно:**
- Когда именно выполнять hedge swap? (при открытии позиции? при движении цены?)
- Какой slippage использовать?
- Нужно ли использовать Jupiter Limit Orders для точности?
- Как обрабатывать множественные hedge swaps?

**Где в коде:** `src/position-monitoring/monitor.ts:200-220`

### 4. **Обработка множественных транзакций**

**Проблема:** SDK может возвращать массив транзакций, сейчас обрабатывается только первая.

**Что нужно:**
- Как обрабатывать массив транзакций? (последовательно? параллельно?)
- Нужна ли атомарность (все или ничего)?

**Где в коде:** `src/dex/meteora.ts:182-197`, `src/dex/meteora.ts:224-234`

### 5. **Улучшение расчета стоимости позиции**

**Проблема:** Используется упрощенная модель (50/50 распределение).

**Что нужно:**
- Как получить реальное распределение токенов по bins из позиции?
- Как рассчитать стоимость позиции с учетом реального распределения?

**Где в коде:** `src/position-monitoring/strategyCalculator.ts:50-60`

### 6. **Интеграция в основной цикл**

**Проблема:** Модуль создан, но не интегрирован в `src/index.ts`.

**Что нужно:**
- Запускать мониторинг вместе с copy-trading ботом?
- Или это отдельный процесс?
- Нужен ли отдельный entry point?

**Где:** `src/index.ts` - нужно добавить инициализацию монитора

### 7. **Конфигурация админа**

**Проблема:** Конфигурация загружается из кода, но нет UI для админа.

**Что нужно:**
- Нужен ли веб-интерфейс для настройки параметров?
- Или достаточно API endpoints?
- Где хранить конфигурацию? (файл, БД, переменные окружения?)

**Где:** `src/position-monitoring/config.ts:40-50`

## ❓ Вопросы для уточнения

1. **Когда открывать новую позицию ниже?**
   - Сразу после того, как решили оставить старую?
   - Или нужно дождаться определенного условия?

2. **Что делать со старой позицией?**
   - Оставлять до достижения TP (±2%)?
   - Или есть другие условия закрытия?

3. **Mirror Swapping:**
   - Выполнять hedge swap сразу при открытии позиции?
   - Или только при движении цены?
   - Как часто обновлять hedge?

4. **Обработка ошибок:**
   - Что делать, если транзакция закрытия не прошла?
   - Retry логика?
   - Уведомления админу?

5. **Множественные позиции:**
   - Сколько позиций можно мониторить одновременно?
   - Есть ли лимиты?

## 📝 Следующие шаги

1. **Получить ответы на вопросы выше**
2. **Реализовать получение реальных данных о комиссиях и пулах**
3. **Доработать Mirror Swapping (выполнение swap)**
4. **Интегрировать в основной цикл бота**
5. **Добавить UI для админа (опционально)**
6. **Тестирование на тестовой сети**

## 🎯 Текущий статус

✅ **Архитектура готова** - все модули созданы и структурированы  
✅ **Логика реализована** - все основные алгоритмы на месте  
✅ **API endpoints добавлены** - можно управлять через API  
⚠️ **Требуется доработка** - нужны реальные данные и уточнения  
⚠️ **Требуется тестирование** - нужно протестировать на реальных данных  

---

**Модуль готов к использованию, но требует доработки для production использования.**

